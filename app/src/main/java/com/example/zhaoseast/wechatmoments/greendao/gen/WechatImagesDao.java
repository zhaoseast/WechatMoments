package com.example.zhaoseast.wechatmoments.greendao.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.example.zhaoseast.wechatmoments.bean.WechatImages;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "WECHAT_IMAGES".
*/
public class WechatImagesDao extends AbstractDao<WechatImages, String> {

    public static final String TABLENAME = "WECHAT_IMAGES";

    /**
     * Properties of entity WechatImages.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, String.class, "id", true, "ID");
        public final static Property Contentid = new Property(1, String.class, "contentid", false, "CONTENTID");
        public final static Property Url = new Property(2, String.class, "url", false, "URL");
        public final static Property Filetype = new Property(3, String.class, "filetype", false, "FILETYPE");
        public final static Property Orderby = new Property(4, String.class, "orderby", false, "ORDERBY");
        public final static Property Creationdate = new Property(5, String.class, "creationdate", false, "CREATIONDATE");
    }


    public WechatImagesDao(DaoConfig config) {
        super(config);
    }
    
    public WechatImagesDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"WECHAT_IMAGES\" (" + //
                "\"ID\" TEXT PRIMARY KEY NOT NULL ," + // 0: id
                "\"CONTENTID\" TEXT," + // 1: contentid
                "\"URL\" TEXT," + // 2: url
                "\"FILETYPE\" TEXT," + // 3: filetype
                "\"ORDERBY\" TEXT," + // 4: orderby
                "\"CREATIONDATE\" TEXT);"); // 5: creationdate
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"WECHAT_IMAGES\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, WechatImages entity) {
        stmt.clearBindings();
 
        String id = entity.getId();
        if (id != null) {
            stmt.bindString(1, id);
        }
 
        String contentid = entity.getContentid();
        if (contentid != null) {
            stmt.bindString(2, contentid);
        }
 
        String url = entity.getUrl();
        if (url != null) {
            stmt.bindString(3, url);
        }
 
        String filetype = entity.getFiletype();
        if (filetype != null) {
            stmt.bindString(4, filetype);
        }
 
        String orderby = entity.getOrderby();
        if (orderby != null) {
            stmt.bindString(5, orderby);
        }
 
        String creationdate = entity.getCreationdate();
        if (creationdate != null) {
            stmt.bindString(6, creationdate);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, WechatImages entity) {
        stmt.clearBindings();
 
        String id = entity.getId();
        if (id != null) {
            stmt.bindString(1, id);
        }
 
        String contentid = entity.getContentid();
        if (contentid != null) {
            stmt.bindString(2, contentid);
        }
 
        String url = entity.getUrl();
        if (url != null) {
            stmt.bindString(3, url);
        }
 
        String filetype = entity.getFiletype();
        if (filetype != null) {
            stmt.bindString(4, filetype);
        }
 
        String orderby = entity.getOrderby();
        if (orderby != null) {
            stmt.bindString(5, orderby);
        }
 
        String creationdate = entity.getCreationdate();
        if (creationdate != null) {
            stmt.bindString(6, creationdate);
        }
    }

    @Override
    public String readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getString(offset + 0);
    }    

    @Override
    public WechatImages readEntity(Cursor cursor, int offset) {
        WechatImages entity = new WechatImages( //
            cursor.isNull(offset + 0) ? null : cursor.getString(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // contentid
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // url
            cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3), // filetype
            cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4), // orderby
            cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5) // creationdate
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, WechatImages entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getString(offset + 0));
        entity.setContentid(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setUrl(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setFiletype(cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3));
        entity.setOrderby(cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4));
        entity.setCreationdate(cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5));
     }
    
    @Override
    protected final String updateKeyAfterInsert(WechatImages entity, long rowId) {
        return entity.getId();
    }
    
    @Override
    public String getKey(WechatImages entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(WechatImages entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
